# Java_project3

This project is one of the hardest I have incountered. It's my first project to use SINGLY LINKEDLIST. In this project we were tasked to 
code a program that can addFirst(int item), addLast(int item), getFirstElement(), getLastElement() in the list of elements.
